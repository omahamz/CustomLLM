# Updated to versions with broad prebuilt wheel support on recent WSL releases
# to avoid compilation and "--break-system-packages" conflicts while keeping
# Hugging Face CLI and core model stack functional.
# Bump PyTorch suite to a version with current prebuilt wheels for WSL.
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
accelerate==0.27.2
datasets==2.16.1
pyarrow==14.0.2
# Use a release with prebuilt wheels for Python 3.12 to avoid cmake/pkg-config builds
sentencepiece==0.2.0
pyyaml==6.0.1
tqdm==4.66.2
InstructorEmbedding==1.0.1
# Pin to first available release in PyPI listing to avoid missing wheel errors on WSL
faiss-cpu==1.8.0
numpy==1.26.4
pymupdf==1.23.8
huggingface-hub==0.20.2
python-dotenv==1.0.1
psutil==5.9.8
